#ifndef DRUNKARDS_WALK_H
#define DRUNKARDS_WALK_H

#include <vector>

void drunkardsWalk(std::vector<std::vector<int>>& map, int startX, int startY, int steps);
void displayMap(const std::vector<std::vector<int>>& map);

#endif